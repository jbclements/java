(module info (lib "infotab.ss" "setup")
  (define name "inspector")
  (define blurb
    (list "A library of inspector utilities."))
  (define doc.txt "doc.txt")
  (define categories '(misc))
  (define primary-file "inspector.ss")
  (define version "2"))
